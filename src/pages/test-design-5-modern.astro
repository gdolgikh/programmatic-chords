---
import Layout from '../components/Layout.astro';
import ChordDiagram from '../components/ChordDiagram.astro';

const chord = {
  name: 'C Major',
  quality: 'Major',
  root: 'C',
  frets: [-1, 3, 2, 0, 1, 0],
  fingers: [0, 3, 2, 0, 1, 0],
  barres: [],
  description: 'One of the first chords every guitarist learns. Bright and open, C Major forms the foundation of countless songs in popular music.'
};

const stringNames = ['E', 'A', 'D', 'G', 'B', 'e'];
const fretLabel = (f) => {
  if (f === -1) return '×';
  if (f === 0) return 'Open';
  return `Fret ${f}`;
};
---

<Layout title={`${chord.name} — Design Test 5 (Modern)`}>
  <article class="chord-page">
    <div class="page-header">
      <div class="breadcrumb">All Chords / C Chords / Major</div>
      <h1 class="title">{chord.name}</h1>
      <div class="meta-row">
        <span class="meta-badge star">⭐ Most Useful</span>
        <span class="meta-badge beginner">Beginner</span>
        <span class="meta-badge open">No Barres</span>
      </div>
    </div>

    <div class="main-content">
      <aside class="sidebar">
        <div class="sidebar-section diagram-section">
          <h3>Fingering</h3>
          <div class="diagram-box">
            <ChordDiagram
              frets={chord.frets}
              fingers={chord.fingers}
              barres={chord.barres}
              size="large"
            />
          </div>
        </div>

        <div class="sidebar-section quick-stats">
          <h3>Quick Stats</h3>
          <div class="stat">
            <span class="stat-label">Difficulty:</span>
            <div class="stat-bar">
              <div class="stat-fill" style="width: 20%;"></div>
            </div>
          </div>
          <div class="stat">
            <span class="stat-label">Finger Spread:</span>
            <div class="stat-bar">
              <div class="stat-fill" style="width: 30%;"></div>
            </div>
          </div>
          <div class="stat">
            <span class="stat-label">Popularity:</span>
            <div class="stat-bar">
              <div class="stat-fill" style="width: 100%;"></div>
            </div>
          </div>
        </div>
      </aside>

      <main class="main-column">
        <section class="intro-section">
          <p class="description">{chord.description}</p>
        </section>

        <section class="section">
          <h2>How to Play</h2>
          <div class="instructions">
            <p class="instruction-step"><strong>Step 1:</strong> Place your middle finger on the 2nd fret of the A string</p>
            <p class="instruction-step"><strong>Step 2:</strong> Place your index finger on the 1st fret of the B string</p>
            <p class="instruction-step"><strong>Step 3:</strong> Place your ring finger on the 3rd fret of the D string</p>
            <p class="instruction-step"><strong>Step 4:</strong> Strum from the A string down, letting E and high e ring open</p>
          </div>
        </section>

        <section class="section">
          <h2>String Breakdown</h2>
          <div class="string-breakdown">
            {chord.frets.map((f, i) => (
              <div class:list={['string-card', { muted: f === -1 }]}>
                <div class="string-name">{stringNames[i]}</div>
                <div class="string-fret">{fretLabel(f)}</div>
                <div class="string-note">
                  {f === -1 ? 'Muted' : f === 0 ? 'Open' : `${stringNames[i]}${f}`}
                </div>
              </div>
            ))}
          </div>
        </section>

        <section class="section">
          <h2>Theory Deep Dive</h2>
          <div class="theory-content">
            <div class="theory-item">
              <h4>Notes in This Chord</h4>
              <p>The C Major chord consists of three notes: <strong>C</strong> (root), <strong>E</strong> (major third), and <strong>G</strong> (perfect fifth). This combination creates a bright, open sound that's the foundation of Western music.</p>
            </div>
            <div class="theory-item">
              <h4>Why This Voicing?</h4>
              <p>This particular fingering is called an "open position" chord because the top two strings ring open without being fretted. It's perfect for beginners and sounds warm and ringing.</p>
            </div>
            <div class="theory-item">
              <h4>Common Progressions</h4>
              <p><strong>I-IV-V:</strong> C - F - G (most common progression)</p>
              <p><strong>vi-IV-I-V:</strong> Am - F - C - G (very popular in pop)</p>
            </div>
          </div>
        </section>

        <section class="section">
          <h2>Practice Tips</h2>
          <ul class="tips-grid">
            <li>
              <span class="tip-emoji">💪</span>
              <div>
                <strong>Build finger strength</strong>
                <p>Hold this chord for 30 seconds, rest 10, repeat 5 times</p>
              </div>
            </li>
            <li>
              <span class="tip-emoji">🔄</span>
              <div>
                <strong>Practice transitions</strong>
                <p>Switch between C, Am, and G to build muscle memory</p>
              </div>
            </li>
            <li>
              <span class="tip-emoji">🎵</span>
              <div>
                <strong>Play along</strong>
                <p>Find songs with C Major and practice playing them</p>
              </div>
            </li>
            <li>
              <span class="tip-emoji">⏱️</span>
              <div>
                <strong>Daily practice</strong>
                <p>15 minutes a day beats 2 hours once a week</p>
              </div>
            </li>
          </ul>
        </section>

        <section class="section">
          <h2>In Popular Music</h2>
          <div class="songs-showcase">
            <div class="song">
              <div class="song-cover">🎸</div>
              <div class="song-info">
                <strong>Knockin' on Heaven's Door</strong>
                <p>Bob Dylan</p>
              </div>
            </div>
            <div class="song">
              <div class="song-cover">🎸</div>
              <div class="song-info">
                <strong>Wonderwall</strong>
                <p>Oasis</p>
              </div>
            </div>
            <div class="song">
              <div class="song-cover">🎸</div>
              <div class="song-info">
                <strong>Wonderful Tonight</strong>
                <p>Eric Clapton</p>
              </div>
            </div>
          </div>
        </section>
      </main>
    </div>

    <nav class="nav-back">
      <a href="/">← Back to all chords</a>
    </nav>
  </article>
</Layout>

<style>
  .chord-page {
    padding: 1rem 0;
  }

  .page-header {
    margin-bottom: 2.5rem;
    padding-bottom: 2rem;
    border-bottom: 2px solid rgba(255, 255, 255, 0.08);
  }

  .breadcrumb {
    font-size: 0.85rem;
    color: rgba(255, 255, 255, 0.5);
    margin-bottom: 1rem;
  }

  .page-header .title {
    font-size: 3.5rem;
    font-weight: 900;
    margin: 0.5rem 0;
    background: linear-gradient(135deg, #a78bfa, #7c3aed);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .meta-row {
    display: flex;
    gap: 0.75rem;
    flex-wrap: wrap;
  }

  .meta-badge {
    font-size: 0.8rem;
    font-weight: 700;
    padding: 0.4rem 1rem;
    border-radius: 20px;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .meta-badge.star {
    background: rgba(34, 197, 94, 0.15);
    color: #86efac;
  }

  .meta-badge.beginner {
    background: rgba(59, 130, 246, 0.15);
    color: #93c5fd;
  }

  .meta-badge.open {
    background: rgba(167, 139, 250, 0.15);
    color: #d8b4fe;
  }

  .main-content {
    display: grid;
    grid-template-columns: 320px 1fr;
    gap: 3rem;
  }

  .sidebar {
    position: sticky;
    top: 100px;
    height: fit-content;
  }

  .sidebar-section {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 12px;
    padding: 1.5rem;
    margin-bottom: 1.5rem;
  }

  .sidebar-section h3 {
    font-size: 0.9rem;
    font-weight: 700;
    text-transform: uppercase;
    color: #a78bfa;
    margin: 0 0 1rem 0;
    letter-spacing: 0.05em;
  }

  .diagram-box {
    display: flex;
    justify-content: center;
    padding: 1rem;
  }

  .stat {
    margin-bottom: 1.25rem;
  }

  .stat:last-child {
    margin-bottom: 0;
  }

  .stat-label {
    display: block;
    font-size: 0.85rem;
    font-weight: 600;
    margin-bottom: 0.4rem;
    color: rgba(255, 255, 255, 0.8);
  }

  .stat-bar {
    height: 6px;
    background: rgba(255, 255, 255, 0.1);
    border-radius: 3px;
    overflow: hidden;
  }

  .stat-fill {
    height: 100%;
    background: linear-gradient(90deg, #a78bfa, #7c3aed);
  }

  .main-column {
    min-width: 0;
  }

  .intro-section {
    margin-bottom: 3rem;
  }

  .description {
    font-size: 1.1rem;
    line-height: 1.8;
    color: rgba(255, 255, 255, 0.8);
    margin: 0;
  }

  .section {
    margin-bottom: 3rem;
  }

  .section h2 {
    font-size: 1.5rem;
    font-weight: 700;
    margin: 0 0 1.5rem 0;
    color: white;
  }

  .instructions {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .instruction-step {
    padding: 1rem;
    background: rgba(167, 139, 250, 0.08);
    border-left: 3px solid #a78bfa;
    border-radius: 6px;
    color: rgba(255, 255, 255, 0.85);
    line-height: 1.6;
    margin: 0;
  }

  .string-breakdown {
    display: grid;
    grid-template-columns: repeat(6, 1fr);
    gap: 1rem;
  }

  .string-card {
    text-align: center;
    padding: 1.25rem 0.75rem;
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 10px;
  }

  .string-card.muted {
    opacity: 0.4;
  }

  .string-name {
    font-weight: 700;
    color: #a78bfa;
    font-size: 1rem;
    margin-bottom: 0.4rem;
  }

  .string-fret {
    font-size: 1.25rem;
    font-weight: 700;
    color: rgba(255, 255, 255, 0.9);
    margin-bottom: 0.3rem;
  }

  .string-note {
    font-size: 0.75rem;
    color: rgba(255, 255, 255, 0.5);
  }

  .theory-content {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .theory-item h4 {
    margin: 0 0 0.5rem 0;
    color: #a78bfa;
    font-weight: 700;
    font-size: 1rem;
  }

  .theory-item p {
    margin: 0 0 0.5rem 0;
    color: rgba(255, 255, 255, 0.8);
    line-height: 1.6;
  }

  .theory-item p:last-child {
    margin: 0;
  }

  .tips-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.5rem;
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .tips-grid li {
    display: flex;
    gap: 1rem;
    padding: 1.25rem;
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 10px;
  }

  .tip-emoji {
    font-size: 1.5rem;
    flex-shrink: 0;
  }

  .tips-grid li strong {
    display: block;
    color: white;
    margin-bottom: 0.3rem;
  }

  .tips-grid li p {
    margin: 0;
    font-size: 0.9rem;
    color: rgba(255, 255, 255, 0.7);
    line-height: 1.5;
  }

  .songs-showcase {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
  }

  .song {
    display: flex;
    gap: 1rem;
    padding: 1.25rem;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 10px;
    align-items: center;
  }

  .song-cover {
    font-size: 2rem;
    flex-shrink: 0;
  }

  .song-info strong {
    display: block;
    color: white;
    margin-bottom: 0.2rem;
  }

  .song-info p {
    margin: 0;
    font-size: 0.85rem;
    color: rgba(255, 255, 255, 0.6);
  }

  .nav-back {
    margin-top: 3rem;
  }

  .nav-back a {
    color: #a78bfa;
    text-decoration: none;
  }

  .nav-back a:hover {
    text-decoration: underline;
  }

  @media (max-width: 1024px) {
    .main-content {
      grid-template-columns: 1fr;
    }

    .sidebar {
      position: static;
    }
  }

  @media (max-width: 768px) {
    .page-header .title {
      font-size: 2.5rem;
    }

    .string-breakdown {
      grid-template-columns: repeat(3, 1fr);
    }

    .tips-grid {
      grid-template-columns: 1fr;
    }

    .songs-showcase {
      grid-template-columns: 1fr;
    }
  }
</style>
