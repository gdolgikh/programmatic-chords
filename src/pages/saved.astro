---
import Layout from '../components/Layout.astro';
import SavedContent from '../components/pages/SavedContent.astro';
import { t } from '../i18n/index.js';
---

<Layout
  locale="en"
  title={`${t('en', 'savedChordsTitle')} — Chord Library`}
  description={t('en', 'savedDescription')}
  enSlug="saved"
  pageType="static"
>
  <SavedContent locale="en" />
</Layout>
